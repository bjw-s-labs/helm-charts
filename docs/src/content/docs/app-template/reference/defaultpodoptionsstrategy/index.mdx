---
title: "defaultPodOptionsStrategy"
description: "Set the strategy for the default pod options. Defaults to overwrite. overwrite: If pod-level opti..."
sidebar:
  label: "defaultPodOptionsStrategy"
---

Set the strategy for the default pod options. Defaults to overwrite.
overwrite: If pod-level options are set, use those instead of the defaults.
merge: If pod-level options are set, merge them with the defaults.

## Type

`string`

**Allowed values:** `overwrite`, `merge`

## Default

```yaml
overwrite
```
