---
controllers:
  main:
    type: deployment
    containers:
      main:
        image:
          repository: nginx
          tag: latest
    pod:
      schedulingGates:
        - name: "example.com/quota-check"
        - name: "example.com/resource-ready"

service:
  main:
    ports:
      http:
        port: 80
